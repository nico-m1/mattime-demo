/**
 * Tailwind CSS configuration for the Mattime project. This file tells
 * Tailwind which files to scan for class names so it can generate the
 * appropriate CSS. Feel free to customize the theme or add plugins
 * as you build out the application.
 */
module.exports = {
  content: [
    './index.html',
    './src/**/*.{js,jsx,ts,tsx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
