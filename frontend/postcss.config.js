/**
 * PostCSS configuration for the Mattime project. It hooks Tailwind and
 * Autoprefixer into the PostCSS pipeline so that your CSS is
 * automatically processed. Adjust this file if you add other
 * PostCSS plugins in the future.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
